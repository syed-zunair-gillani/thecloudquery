/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['staging.thecloudquery.com', 'res.cloudinary.com', 'www.hochzeitsmodus.de'], // Allow images from localhost
    },
  };
  
  export default nextConfig;